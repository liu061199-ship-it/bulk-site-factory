# Optional: choose which generated site to preview locally.
# Use a site id from sites/sites.json, for example "alpha-studio".
SITE_ID=alpha-studio

# Optional: used by deployment platforms when host detection is unavailable.
NEXT_PUBLIC_SITE_DOMAIN=alpha.example.com
